FROM ubuntu:16.04

# 'language-pack-sv' is needed for locale tests
RUN apt-get update && apt-get install -y \
    build-essential \
    cmake \
    doxygen \
    vim \
    language-pack-sv \
    valgrind
